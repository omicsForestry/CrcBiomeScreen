% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PredictCrcBiomeScreen.R
\name{PredictCrcBiomeScreen}
\alias{PredictCrcBiomeScreen}
\title{Predict the class and probabilities for new data}
\usage{
PredictCrcBiomeScreen(
  CrcBiomeScreenObject,
  newdata,
  model_type = c("RF", "XGBoost")
)
}
\arguments{
\item{CrcBiomeScreenObject}{The object containing the trained model.}

\item{newdata}{The data frame or matrix of new features to predict on.}

\item{model_type}{The type of model to use for prediction ("RF" or "XGBoost").}
}
\value{
A \linkS4class{CrcBiomeScreenObject} with a data frame containing sample-specific predictions.
}
\description{
Predict the class and probabilities for new data
}
\examples{
# --- Minimal runnable example ---

# Create a tiny toy dataset (2 samples × 2 features)
newdata <- data.frame(
  Feature1 = c(0.2, 0.8),
  Feature2 = c(0.7, 0.3)
)
rownames(newdata) <- c("S1", "S2")

# Create a minimal CrcBiomeScreen object
toy_obj <- new(
  "CrcBiomeScreen",
  AbsoluteAbundance = data.frame(),
  RelativeAbundance = data.frame(),
  TaxaData = data.frame(),
  SampleData = data.frame(),
  ModelResult = list(),
  EvaluateResult = list()
)

if (interactive()) {
  pred_obj <- PredictCrcBiomeScreen(
    CrcBiomeScreenObject = toy_obj,
    newdata = newdata,
    model_type = "RF"
  )

  print(getPredictResult(pred_obj)$RF)
}
}
