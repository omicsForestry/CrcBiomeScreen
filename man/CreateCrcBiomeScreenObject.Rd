% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CreateCrcBiomeScreenObject.R
\name{CreateCrcBiomeScreenObject}
\alias{CreateCrcBiomeScreenObject}
\title{Create the CrcBiomeScreenObject for analysis
This function creates the CrcBiomeScreenObject to contain the data for analysis.}
\usage{
CreateCrcBiomeScreenObject(
  AbsoluteAbundance = NULL,
  TaxaData = NULL,
  SampleData = NULL,
  RelativeAbundance = NULL
)
}
\arguments{
\item{AbsoluteAbundance}{A numeric matrix or data frame containing absolute abundance data}

\item{TaxaData}{A data frame containing taxonomic information for each feature}

\item{SampleData}{A data frame containing sample metadata}

\item{RelativeAbundance}{A numeric matrix or data frame containing relative abundance data}
}
\value{
An object with data
}
\description{
Create the CrcBiomeScreenObject for analysis
This function creates the CrcBiomeScreenObject to contain the data for analysis.
}
\details{
The \code{CrcBiomeScreenObject} is a list-based S3 object designed to store all data, models, and results throughout the analysis workflow. This ensures consistency and makes it easy to track changes. The object is initialized with several key slots, and other slots are populated by subsequent functions.

\describe{
\item{\strong{AbsoluteAbundance}}{A data frame containing absolute abundance data (counts).}
\item{\strong{TaxaData}}{A data frame mapping features to their taxonomic information.}
\item{\strong{SampleData}}{A data frame containing sample metadata, such as clinical status or the number of sequencing reads.}
\item{\strong{RelativeAbundance}}{A data frame containing relative abundance data.}
\item{\strong{TaxaLevelData}}{A list of data frames containing abundance data aggregated to a specific taxonomic level (e.g., genus, family). Populated by \code{KeepTaxonomicLevel}.}
\item{\strong{NormalizedData}}{A data frame containing the normalized abundance data. Populated by \code{NormalizeData}.}
\item{\strong{ValidationData}}{A list containing the processed external validation data.}
\item{\strong{ModelData}}{A list containing the processed training and test data, ready for model training. Populated by \code{SplitData}.}
\item{\strong{ModelResult}}{A list storing the trained model objects (e.g., \code{randomForest} or \code{xgboost} objects). Populated by \code{TrainModel}.}
\item{\strong{EvaluateResult}}{A list storing performance metrics (e.g., AUC, ROC curve) for the models on the test set. Populated by \code{EvaluateModel}.}
\item{\strong{PredictResult}}{A list storing prediction probabilities and evaluation metrics for external validation sets. Populated by \code{ValidateModelOnData}.}
}
}
\examples{
toydata <- curatedMetagenomicData("ThomasAM_2018a.relative_abundance"
                                , dryrun = FALSE, rownames = "short")

CrcBiomeScreenObject <- CreateCrcBiomeScreenObject(
  RelativeAbundance = toydata[[1]]@assays@data@listData$relative_abundance,
  TaxaData = toydata[[1]]@rowLinks$nodeLab,
  SampleData = toydata[[1]]@colData
)

}
